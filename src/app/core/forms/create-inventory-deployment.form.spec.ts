import { CreateInventoryDeploymentForm } from './create-inventory-deployment.form';

describe('CreateInventoryDeploymentForm', () => {
  it('should create an instance', () => {
    expect(new CreateInventoryDeploymentForm()).toBeTruthy();
  });
});
