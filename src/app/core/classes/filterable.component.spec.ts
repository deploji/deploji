import { FilterableComponent } from './filterable.component';

describe('Filterable Component', () => {
  it('should instantiate', () => {
    const component = new FilterableComponent();
    expect(component).toBeTruthy();
  });
});
